# APP_FLOW — 归处 Guichu 用户流程

> 每一个屏幕、每一个跳转、每一个决策点都用 plain 中文写下。AI 不需要猜用户怎么走路。

## 0. 屏幕清单与路由表

| 路由 | 屏幕 | 用途 |
|------|------|------|
| `/` | **Home（主页）** | 一个超大圆形按钮"📍 记录此地" + 底部 tab |
| `/ancestors` | **长辈列表** | 已记录的长辈卡片流 |
| `/ancestors/[id]` | **长辈详情** | 单个长辈：墓地、对话、访谈、声音 |
| `/ancestors/[id]/interview` | **访谈页** | 引导式问答录音 |
| `/ancestors/[id]/chat` | **对话页** | 与数字人格对话 |
| `/ancestors/[id]/route` | **寻路指南** | 图文路线列表 / 步进模式 |
| `/ancestors/[id]/route/record` | **记录路线** | 路线记录模式（全屏） |
| `/ancestors/new` | **新建长辈** | 表单：姓名、与我的关系、生卒年、去世日期 |
| `/ancestors/[id]/rituals` | **习俗日历** | 该长辈所有重要日子时间线 |
| `/ancestors/[id]/rituals/[key]` | **习俗详情** | 单个习俗：做什么、带什么、注意什么 |
| `/tree` | **族谱树** | SVG 家谱可视化 |
| `/settings` | **设置主页** | 入口 |
| `/settings/region` | **地区设置** | 省 → 市两级选择，影响习俗内容 |
| `/settings/llm` | **LLM 配置** | baseURL + apiKey + model |
| `/settings/tts` | **TTS 配置** | 引擎选择 + key |
| `/settings/about` | **关于** | 版本、license、致谢、隐私声明 |

底部 tab：`Home` / `Ancestors` / `Tree` / `Settings`（4 个）

## 1. 首次启动流程（First Run）

```
启动 App
  ↓
检查 SQLite 是否存在
  ↓
  ├─ 不存在 → 初始化数据库 → 显示「欢迎页」
  │           ↓
  │           「欢迎页」一句话："在你还来得及的时候，把他们留下来。"
  │           ↓
  │           按钮：「开始」
  │           ↓
  │           请求权限：定位（必需）、麦克风（必需）、相机（可选）、相册（可选）
  │           ↓
  │           跳转到 Home
  │
  └─ 已存在 → 直接进入 Home
```

**决策点**：
- 用户拒绝定位权限 → 仍可进入 App，但 Home 大按钮变灰，提示"需要定位权限才能记录坟墓"
- 用户拒绝麦克风权限 → 访谈页禁用录音，但允许手动文字输入

## 2. F1 — 记录坟墓位置（核心路径，必须 60 秒可达）

```
[Home 大按钮]
  ↓ 用户长按 200ms（或单击）
  ↓ 触发 expo-haptics 中等震动
  ↓
[定位中... 蒙层]
  ├─ 显示当前 GPS 精度（米）
  ├─ 倒计时 3 秒
  ↓
  ├─ 精度 ≤10 米 → 自动确认 → 进入下一屏
  └─ 精度 >10 米 或超时 → 弹「精度不够，是否仍然保存？」
                         ↓
                         ├─ 是 → 进入下一屏
                         └─ 否 → 返回 Home，显示"再试一次"
  ↓
[确认页]
  ┌─────────────────────┐
  │  📍 已记录位置       │
  │  纬度: 36.123456    │
  │  经度: 119.123456   │
  │  精度: 5.2 米        │
  ├─────────────────────┤
  │  关联到谁？           │
  │  [+ 选择长辈] (可跳过)│
  ├─────────────────────┤
  │  [📷 加照片]         │
  │  [🎤 加语音备注]     │
  ├─────────────────────┤
  │  [保存]              │
  └─────────────────────┘
  ↓
点击「保存」
  ↓
写入 SQLite
  ↓
触觉反馈 + Toast「已保存」
  ↓
返回 Home（地点已加入"我的位置"列表，主页底部出现一个小气泡显示"今天记录了 1 个位置"）
```

**错误处理**：
- 写入 SQLite 失败 → Toast「保存失败，请重试」+ 数据缓存到内存，下次启动时再次尝试
- 用户中途返回 → 暂存的 GPS 坐标 30 秒内有效，可直接恢复

## 3. F2 — 寻路指南 + 导航

### 3a. 记录路线（在扫墓现场使用）

```
[长辈详情页]
  ↓ 点击「🗺️ 记录寻路路线」
  ↓
[路线记录模式 — 全屏]
  ┌─────────────────────────┐
  │  正在为 祖父 记录路线     │
  │  已记录 0 个路线点        │
  │                          │
  │  💡 从你下车/进村的地方    │
  │     开始，沿路走向墓地    │
  │     每到一个岔路口或标志  │
  │     物就按一下记录         │
  │                          │
  │  ┌────────────────┐     │
  │  │  📌 记录路线点  │     │  ← 底部常驻大按钮
  │  └────────────────┘     │
  │  [结束路线]              │
  └─────────────────────────┘
  ↓ 用户走到一个岔路口，按「📌 记录路线点」
  ↓
[路线点录入 — 半屏弹窗]
  ┌─────────────────────────┐
  │  路线点 ①                │
  │  📍 36.123, 119.456      │
  │  ──────────────          │
  │  [📷 拍一张] [🖼️ 从相册选]│  ← 至少 1 张照片（必选）
  │  [已选 1 张照片 ✓]       │
  │  ──────────────          │
  │  备注 (选填，≤50 字)     │
  │  ┌──────────────┐       │
  │  │ 这里右转上坡  │       │
  │  └──────────────┘       │
  │  [🎤 加语音备注]         │
  │  ──────────────          │
  │  [保存这个点]            │
  └─────────────────────────┘
  ↓ 保存
  ↓ 写入 route_waypoints 表，sort_order 自增
  ↓ 返回路线记录模式，计数器变为"已记录 1 个路线点"
  ↓
用户继续走…重复上述步骤…
  ↓
到达坟墓，按「📌 记录路线点」最后一个点
  ↓ 或按「结束路线」
  ↓
[结束确认]
  ┌─────────────────────────┐
  │  路线记录完成！            │
  │  共 5 个路线点             │
  │  ──────────────          │
  │  [预览路线] [保存并退出]  │
  └─────────────────────────┘
```

### 3b. 查看 & 寻路（下次回来使用）

```
[长辈详情页]
  ↓ 点击「🗺️ 寻路指南」卡片
  ↓
[寻路指南页 — 竖向图文列表]
  ┌─────────────────────────┐
  │  到 祖父 墓地的路线       │
  │  共 5 个路线点            │
  ├─────────────────────────┤
  │  ① 村口下车点              │
  │  ┌───────────────┐      │
  │  │  [大图：村口照片]│      │  ← 可左右滑动多张
  │  └───────────────┘      │
  │  📝 "到村口后朝北走"      │
  │  🔊 [播放语音]            │
  ├─────────────────────────┤
  │       ↓ 约 200 米         │  ← 两点间距离（GPS 计算）
  ├─────────────────────────┤
  │  ② 大柳树岔路口            │
  │  ┌───────────────┐      │
  │  │  [大图：柳树照片]│      │
  │  └───────────────┘      │
  │  📝 "看到大柳树右转"      │
  ├─────────────────────────┤
  │       ↓ 约 150 米         │
  ├─────────────────────────┤
  │  ③ ...                    │
  │  ④ ...                    │
  ├─────────────────────────┤
  │  ⑤ 📍 终点：祖父墓地      │
  │  ┌───────────────┐      │
  │  │  [大图：墓碑照片]│      │
  │  └───────────────┘      │
  │  📝 "红砖房后面山坡上"    │
  ├─────────────────────────┤
  │  [🧭 导航到起点]          │  ← 调起地图 App 导航到第①个点
  │  [🧭 导航到终点]          │  ← 调起地图 App 导航到墓地
  │  [✏️ 编辑路线]            │
  └─────────────────────────┘
```

### 3c. 寻路步进模式（边走边看）

```
[寻路指南页]
  ↓ 点击「🚶 开始寻路」
  ↓
[步进模式 — 全屏卡片]
  ┌─────────────────────────┐
  │  ① / 5                   │
  │                          │
  │  ┌───────────────────┐  │
  │  │                   │  │
  │  │  [全屏大图]        │  │  ← 尽量大，让用户对照实景
  │  │                   │  │
  │  └───────────────────┘  │
  │                          │
  │  到村口后朝北走            │
  │  🔊 [播放语音]            │
  │                          │
  │  [← 上一步] [下一步 →]   │
  └─────────────────────────┘
  ↓ 用户到了，点「下一步」
  ↓ 切到下一个路线点卡片
  ↓ …
  ↓ 最后一步
  ┌─────────────────────────┐
  │  ⑤ / 5 · 终点 📍         │
  │  [全屏大图：墓碑照片]    │
  │  红砖房后面山坡上          │
  │  🎉 你到了                │
  └─────────────────────────┘
```

### 3d. 调起第三方地图导航

```
用户点「🧭 导航到起点」或「🧭 导航到终点」
  ↓
检查用户在「设置 → 默认地图 App」的偏好
  ├─ Apple Maps（iOS 默认）→ expo-linking 调起 maps://...
  ├─ 高德地图 → 调起 androidamap://...
  ├─ 百度地图 → 调起 baidumap://...
  ├─ Google Maps → 调起 https://maps.google.com/...
  └─ 用户没设置 → 弹出 ActionSheet 让用户选一次（记住选择）
  ↓
跳出归处，进入第三方地图 App 开始导航
```

## 4. F3 — 蒸馏长辈 .skill（引导式访谈）

```
[长辈详情页]
  ↓ 点击「📖 开始访谈」
  ↓
[访谈准备页]
  ┌─────────────────────┐
  │  访谈奶奶              │
  │  共 5 个主题，20 个问题│
  │  预计 30-60 分钟       │
  │  ☑ 童年              │
  │  ☑ 爱情与婚姻         │
  │  ☑ 子女与家庭         │
  │  ☑ 人生遗憾           │
  │  ☑ 给后人的话         │
  │  [+ 添加自定义问题]   │
  │  [开始访谈]           │
  └─────────────────────┘
  ↓
[访谈进行页]
  ┌─────────────────────┐
  │  问题 3 / 20         │
  │  ─────────────       │
  │  「您小时候最深的    │
  │   一个回忆是什么？」  │
  │  ─────────────       │
  │  [⏺ 长按录音] (松开停止)│
  │  录音中... 0:23       │
  │  ─────────────       │
  │  [跳过] [上一题] [下一题] │
  └─────────────────────┘
  ↓ 每个回答录完
  ↓ 后台异步：保存音频文件 + （若 ASR 已配置）调用转写
  ↓
全部完成 → [蒸馏页]
  ┌─────────────────────┐
  │  正在蒸馏奶奶的人格...│
  │  ▓▓▓▓▓▓▓▓░░ 80%      │
  │  调用 deepseek-chat   │
  └─────────────────────┘
  ↓
LLM 拼接 prompt:
  - 系统提示：「你正在为一位用户整理她的奶奶的'数字人格档案'。基于以下访谈内容，输出一份符合 Anthropic Skills 规范的 .skill 文件…」
  - 用户消息：所有问答的转写文本
  ↓
返回 .skill 文件内容
  ↓
[预览页]
  ┌─────────────────────┐
  │  奶奶.skill 已生成    │
  │  ┌─────────────┐   │
  │  │ Markdown 渲染 │   │
  │  └─────────────┘   │
  │  [✏️ 编辑]           │
  │  [💾 保存到本人]     │
  │  [⬇ 导出文件]        │
  └─────────────────────┘
  ↓
保存到 ancestors.skill_content 字段
```

**错误处理**：
- 用户没配 LLM key → 跳转到 `/settings/llm`，配置后回到这一步
- LLM 调用失败 → 显示错误 + 「重试」/「保存原始访谈以后再蒸馏」

## 5. F4 — 与长辈对话

```
[长辈详情页]
  ↓ 点击「💬 和奶奶聊聊」
  ↓
[首次进入] 强制弹窗
  ┌─────────────────────┐
  │  ⚠️ 重要提示          │
  │  这是一个基于你记录   │
  │  的内容生成的数字人格 │
  │  ，不是真人。它无法   │
  │  回答你未曾记录的事。 │
  │  [我明白了]          │
  └─────────────────────┘
  ↓
[对话页]
  ┌─────────────────────┐
  │  奶奶                 │
  │  ─────────────       │
  │  奶奶: 「来啦小宝，   │
  │   今儿吃饭了没？」    │
  │                      │
  │  我: 「奶奶，最近    │
  │   工作压力大」        │
  │                      │
  │  奶奶: ...           │
  ├─────────────────────┤
  │  [输入] [🎤] [➤]    │
  │  [🔊 用奶奶的声音念]  │
  └─────────────────────┘
  ↓
用户发送消息
  ↓
组装 messages：
  [
    { role: "system", content: 奶奶.skill 全文 + 安全护栏 },
    ...历史 20 轮,
    { role: "user", content: 当前输入 }
  ]
  ↓
调用 LLM (流式)
  ↓
气泡里逐字渲染
  ↓
（若开启声音克隆）流式合成音频，播放
  ↓
保存到 conversations 表
```

## 6. F5 — 声音克隆

```
[长辈详情页]
  ↓ 点击「🎤 训练声音」
  ↓
[声音训练页]
  ┌─────────────────────┐
  │  需要至少 30 秒清晰录音│
  │  当前已收集: 12 秒    │
  │  ─────────────       │
  │  来源：              │
  │  - 访谈中的 8 段录音 │
  │  - 0 段单独录制       │
  │  ─────────────       │
  │  [+ 录一段]          │
  │  [开始训练]          │
  └─────────────────────┘
  ↓ 点击「开始训练」
  ↓
[授权弹窗]
  ┌─────────────────────┐
  │  🛡️ 伦理确认          │
  │  克隆声音是一项严肃   │
  │  的事。请确认：        │
  │  ☐ 我已获得本人或     │
  │     直系亲属授权      │
  │  ☐ 不会用于诈骗、     │
  │     伪证、欺骗他人    │
  │  ☐ 仅用于个人纪念     │
  │  [取消] [确认并继续]  │
  └─────────────────────┘
  ↓
检查 settings.tts.engine
  ├─ system → 提示「系统 TTS 不支持克隆，请到设置切换为 minimax 或 elevenlabs」
  ├─ minimax → 调用 MiniMax voice clone API
  └─ elevenlabs → 调用 ElevenLabs voice clone API
  ↓
返回 voice_id
  ↓
保存到 ancestors.voice_id 字段
  ↓
对话页"🔊 用 xx 的声音念"开关变为可用
```

## 7. F6 — 族谱树

```
[底部 tab → Tree]
  ↓
[族谱树页]
  - SVG 渲染：从最年长的人开始往下
  - 节点形状：方形（男）/ 圆形（女）/ 八边形（不指定）
  - 已离世的节点边框为虚线
  - 有 .skill 的节点右上角小标
  - 有声音的节点右下角小喇叭
  ↓
点击节点 → 跳到 /ancestors/[id]
长按节点 → 弹菜单：「添加父母 / 添加配偶 / 添加子女 / 编辑 / 删除」
```

## 8. F7 — 习俗指南

### 8a. 主页习俗提醒（Home 页面融合）

```
[Home]
  ┌─────────────────────────┐
  │                          │
  │     [📍 记录此地]         │  ← 主按钮
  │                          │
  │  ─────────────────       │
  │  📅 近期提醒              │  ← 主按钮下方卡片区
  │  ┌───────────────────┐  │
  │  │  清明节 · 还有 3 天  │  │
  │  │  祖父张守义          │  │
  │  │  需要准备：纸钱、香   │  │
  │  │  烛、供果、饺子       │  │
  │  │  [查看详情 →]        │  │
  │  └───────────────────┘  │
  │  ┌───────────────────┐  │
  │  │  周年祭 · 还有 18 天 │  │
  │  │  奶奶李秀兰          │  │
  │  │  [查看详情 →]        │  │
  │  └───────────────────┘  │
  └─────────────────────────┘
```

**逻辑**：
- App 启动时扫描所有已设 `death_date` 的长辈
- 计算未来 30 天内的重要日子
- 按时间排序显示前 3 条
- 没有近期提醒时不显示此区域

### 8b. 长辈详情页 — 习俗日历入口

```
[长辈详情页]
  ┌─────────────────────────┐
  │  [头像] 祖父·张守义      │
  │  1935-2024 · 去世第 2 年  │
  ├─────────────────────────┤
  │  📅 习俗日历              │  ← 新增卡片
  │  ┌───────────────────┐  │
  │  │  下一个：清明节      │  │
  │  │  2026-04-04 · 3 天后 │  │
  │  │  需带：纸钱/香烛/供果│  │
  │  └───────────────────┘  │
  │  [查看全部日历 →]        │
  ├─────────────────────────┤
  │  📍 墓地位置 · 🗺️ 寻路指南│
  │  💬 和祖父聊聊            │
  │  📖 访谈记录              │
  │  🎤 声音档案              │
  └─────────────────────────┘
```

### 8c. 习俗日历页（完整时间线）

```
[长辈详情页]
  ↓ 点击「查看全部日历」
  ↓
[习俗日历页]
  ┌─────────────────────────┐
  │  祖父·张守义 · 习俗日历   │
  │  去世日期：2024-09-15     │
  │  地区：山东省·潍坊市      │
  │  ─────────────────       │
  │                          │
  │  ── 已过 ──               │  ← 灰色，可折叠
  │  ✓ 头七 · 2024-09-21     │
  │  ✓ 百日 · 2024-12-24     │
  │  ✓ 周年祭 · 2025-09-15   │
  │                          │
  │  ── 即将到来 ──           │  ← 高亮
  │  → 清明节 · 2026-04-04    │  ← 当前最近的，标记箭头
  │    还有 3 天              │
  │  → 中元节 · 2026-08-27   │
  │                          │
  │  ── 未来 ──               │
  │  ○ 三周年祭 · 2027-09-15 │
  │  ○ 清明节 · 2027-04-05   │
  │  …                       │
  └─────────────────────────┘
  ↓ 点击任一条目
  ↓
[习俗详情页]
```

### 8d. 习俗详情页

```
[习俗详情页]
  ┌─────────────────────────┐
  │  清明节                    │
  │  2026-04-04（农历三月初七）│
  │  祖父·张守义 去世第 2 年   │
  ├─────────────────────────┤
  │  📖 习俗说明              │
  │  清明节是最重要的扫墓节日  │
  │  ─────────────────       │
  │                          │
  │  📋 要做什么              │
  │  ☐ 扫墓、清理坟头杂草     │
  │  ☐ 上香                   │
  │  ☐ 烧纸钱                 │
  │  ☐ 摆放供品               │
  │  ─────────────────       │
  │                          │
  │  🛒 要带什么              │
  │  ☐ 纸钱                   │  ← 可勾选（当作采购清单）
  │  ☐ 香烛                   │
  │  ☐ 供果                   │
  │  ☐ 酒                     │
  │  ☐ 饺子 🏷️潍坊习俗        │  ← 地方特色标注
  │  ☐ 单饼 🏷️潍坊习俗        │
  │  ☐ 鲜花                   │
  │  ─────────────────       │
  │                          │
  │  🏠 家中注意事项           │
  │  • 去世第 2 年春节贴绿色   │
  │    对联（不是红色）        │
  │  ─────────────────       │
  │                          │
  │  [🧭 导航去扫墓]          │  ← 直接跳到 F2 导航
  └─────────────────────────┘
```

**交互**：
- 🛒 清单的勾选状态保存在本地（方便买东西时对着勾）
- 每年清明自动生成新的一条（勾选状态重置）
- 地方特色用 🏷️ 标签标注，与通用习俗区分

### 8e. 首次设置地区

```
首次添加一个有 death_date 的长辈时
  ↓
检查 settings.region 是否已设
  ├─ 已设 → 直接用
  └─ 未设 → 弹窗
      ┌─────────────────────┐
      │  设置你的家乡          │
      │  不同地区的丧葬习俗    │
      │  有差异，选择后会显示  │
      │  更精准的习俗指南      │
      │  ──────────          │
      │  省：[山东省 ▼]       │
      │  市：[潍坊市 ▼]       │
      │  ──────────          │
      │  [跳过（用通用习俗）]  │
      │  [确定]               │
      └─────────────────────┘
  ↓
保存到 MMKV settings.region
  ↓
触发重新计算 ritual_reminders 表
```

## 9. 设置流程

```
[Settings]
  ├─ 我的地区（习俗相关）
  │   ├─ 省 → 市两级选择
  │   ├─ 当前：山东省 · 潍坊市
  │   ├─ [修改] → 重新计算所有习俗日历
  │   └─ 提示文字：影响习俗指南的地方特色内容
  ├─ 习俗提醒
  │   ├─ 开关：开启/关闭本地通知
  │   ├─ 提前天数：7 天 / 3 天 / 1 天（多选）
  │   └─ 提醒时间：上午 9:00（可调）
  ├─ LLM 配置
  │   ├─ Provider 下拉（DeepSeek/Kimi/通义/OpenAI/Claude/Ollama/自定义）
  │   ├─ baseURL 输入框（自动填入对应默认值）
  │   ├─ apiKey 输入框（密码模式，存 expo-secure-store）
  │   ├─ model 输入框（如 deepseek-chat）
  │   ├─ [测试连接] 按钮
  │   └─ [保存]
  ├─ TTS 配置
  │   ├─ Engine 单选（system / minimax / elevenlabs）
  │   ├─ apiKey（仅非 system 时显示）
  │   └─ [保存]
  ├─ 默认地图 App
  │   └─ 单选列表
  ├─ 数据管理
  │   ├─ 导出全部数据为 zip
  │   ├─ 从 zip 恢复
  │   └─ 清空所有数据（双重确认）
  └─ 关于
      ├─ 版本号
      ├─ GitHub 链接
      ├─ License (MIT)
      └─ 隐私声明（再次强调：本 App 无服务端，不收集任何数据）
```

## 9. 错误与边界

| 场景 | 行为 |
|------|------|
| 无网络 | 仍可记录位置/访谈/查看本地数据；对话和声音克隆不可用，提示"功能需要联网" |
| API key 失效 | 红色 banner 提示，引导到设置页 |
| 存储已满 | 阻止录音，提示用户清理 |
| 权限被拒 | 在使用相关功能时引导用户去系统设置开启 |
| App 升级后数据库 schema 变化 | 自动迁移，迁移失败弹提示，引导导出原数据 |
